{
  "name": "ina-platform-api",
  "private": true,
  "description": "Dependencies for the Vercel serverless functions under api/. Not published; the static site itself (index.html, app/, assets/) has zero dependencies and needs no build step.",
  "dependencies": {
    "pdf-parse": "2.4.5",
    "@aws-sdk/client-bedrock-runtime": "^3.1095.0"
  }
}
